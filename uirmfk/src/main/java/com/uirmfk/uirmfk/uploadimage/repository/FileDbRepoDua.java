/*package com.uirmfk.uirmfk.uploadimage.repository;

import com.uirmfk.uirmfk.uploadimage.model.FileDB;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.Optional;

public interface FileDbRepoDua extends JpaRepository<FileDB,Long> {
    Page<FileDB> findByPostId(Long postId, Pageable pageable);
    Optional<FileDB> findByIdAndPostId(Long idd, Long postId);
}

 */
